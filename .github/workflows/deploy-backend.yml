name: Build & Push Backend

on:
  push:
    branches:
      - "**"
    paths:
      - "apps/backend/**"
      - ".github/workflows/apps/backend.yml"
  workflow_dispatch:

permissions:
  contents: read
  packages: write

jobs:
  build-and-push-backend:
    runs-on: ubuntu-latest

    steps:
      - uses: actions/checkout@v4
      - uses: docker/setup-buildx-action@v3
      - uses: docker/login-action@v3
        with:
          registry: ghcr.io
          username: ${{ github.actor }}
          password: ${{ secrets.GITHUB_TOKEN }}

      - name: Determine tags
        id: tags
        run: |
          REPO_LOWER="${GITHUB_REPOSITORY,,}"
          BRANCH_LOWER="${GITHUB_REF_NAME,,}"

          if [ "$BRANCH_LOWER" = "main" ]; then
            echo "TAGS=ghcr.io/$REPO_LOWER/apps/backend:latest,ghcr.io/$REPO_LOWER/apps/backend:$GITHUB_SHA" >> $GITHUB_OUTPUT
          else
            echo "TAGS=ghcr.io/$REPO_LOWER/apps/backend:$BRANCH_LOWER,ghcr.io/$REPO_LOWER/apps/backend:$GITHUB_SHA" >> $GITHUB_OUTPUT
          fi

      - uses: docker/build-push-action@v5
        with:
          context: ./apps/backend
          file: ./apps/backend/Dockerfile
          push: true
          tags: ${{ steps.tags.outputs.TAGS }}
          cache-from: type=registry,ref=ghcr.io/${{ github.repository }}/apps/backend:latest
          cache-to: type=inline
