# Required: Spotify API credentials (get from https://developer.spotify.com/dashboard)
SPOTIFY_CLIENT_ID=your_spotify_client_id
SPOTIFY_CLIENT_SECRET=your_spotify_client_secret
SPOTIFY_REFRESH_TOKEN=your_spotify_refresh_token

# Required: Wakatime API key (get from https://wakatime.com/settings/account)
WAKATIME_API_KEY=your_wakatime_api_key

# Optional: JSON map of device logical names to API keys/tokens
# Example: DEVICES_JSON='{"laptop":"key1","raspberry-pi":"key2"}'
DEVICES_JSON=

# Server Configuration (optional, defaults to 8080)
PORT=8080

# Notes:
# - This file is an example and should NOT contain real secrets.
# - Create a local `.env` with your real values; `.env` is ignored by git.
