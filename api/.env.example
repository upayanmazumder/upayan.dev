# api/.env.example
# Copy this file to `.env` and fill in real values (do NOT commit secrets).

# Required: Spotify API credentials (get from https://developer.spotify.com/dashboard)
SPOTIFY_CLIENT_ID=your_spotify_client_id
SPOTIFY_CLIENT_SECRET=your_spotify_client_secret
# A long-lived refresh token you obtained for your Spotify account
SPOTIFY_REFRESH_TOKEN=your_spotify_refresh_token

# Required: Wakatime API key (get from https://wakatime.com/settings/account)
WAKATIME_API_KEY=your_wakatime_api_key

# Signing key used by the backend (HMAC or similar). This is mandatory.
# Generate a strong random value, e.g. `openssl rand -hex 32` or `pwgen -s 32 1`.
SIGNING_KEY=replace_with_a_strong_random_value_32_bytes_hex

# Optional: JSON map of device logical names to API keys/tokens
# Example (single-line JSON):
# DEVICES_JSON='{"laptop":"key1","raspberry-pi":"key2"}'
DEVICES_JSON=

# Server configuration
# PORT: port the HTTP server listens on (defaults to 8080 in code)
PORT=8080

# Helpful notes:
# - Keep this file as the canonical example. Copy to `.env` for local development.
# - Never check real secrets into source control. Use environment-specific secret
#   storage for production deployments (Azure Key Vault, AWS Secrets Manager, etc.).
